kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pusher-pipeline-claim
  namespace: pusher-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


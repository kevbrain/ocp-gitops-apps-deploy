kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: pusher-dev
  annotations:
    its4u.placeholders/auto: "true"
    reloader.stakater.com/auto: "true"
  name: pusher
  labels:
    app: pusher
spec:
  replicas: {{ocp.replicas}}
  selector:
    matchLabels:
      app: pusher
  template:
    metadata:
      name: pusher
      labels:
        app: pusher
    spec:
      volumes:
        - name: emptydirlogs
          emptyDir: {}
      containers:
        - name: microservice-pod 
          resources:
            requests:
              memory: 250Mi
              cpu : 200m
            limits:
              memory: 750Mi
              cpu : 500m
          readinessProbe:
            httpGet:
              path: actuator/health
              port: 8080
              scheme: HTTP
          env:
            - name: ocp-cluster.registry
              valueFrom:
                configMapKeyRef:
                  name: pusher-cm-0
                  key: ocp-cluster.registry
            - name: ocp.environment
              valueFrom:
                configMapKeyRef:
                  name: pusher-cm-0
                  key: ocp.environment
            - name: ocp-namespace
              valueFrom:
                configMapKeyRef:
                  name: pusher-cm-0
                  key: ocp-namespace
            - name: cluster-suffix
              valueFrom:
                configMapKeyRef:
                  name: pusher-cm-0
                  key: cluster-suffix
            - name: ocp.replicas
              valueFrom:
                configMapKeyRef:
                  name: pusher-cm-0
                  key: ocp.replicas
            - name: app-version
              valueFrom:
                configMapKeyRef:
                  name: pusher-cm-0
                  key: app-version
            - name: actuator.password
              valueFrom:
                secretKeyRef:
                  name: pusher-secret-0
                  key: actuator.password
          ports:
            - containerPort: 8080
              protocol: TCP
              name: http
          imagePullPolicy: Always
          volumeMounts:
            - name: emptydirlogs
              mountPath: /Logs
          image: >-
            {{ocp-cluster.registry}}/{{ocp-namespace}}/pusher:{{app-version}}
      restartPolicy: Always
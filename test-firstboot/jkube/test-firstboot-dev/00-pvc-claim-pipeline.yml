kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-firstboot-pipeline-claim
  namespace: test-firstboot-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


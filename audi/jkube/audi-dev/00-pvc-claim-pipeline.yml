kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: audi-pipeline-claim
  namespace: audi-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


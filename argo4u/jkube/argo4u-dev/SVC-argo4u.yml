kind: Service
apiVersion: v1
metadata:
  namespace: argo4u-dev
  name: argo4u-service
  labels:
    app: argo4u
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: argo4u
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/managed-by: EventListener
    app.kubernetes.io/part-of: Triggers
    eventlistener: surprise
  name: el-surprise
  namespace: surprise-dev
spec:
  host: el-surprise-surprise-dev.apps.ocp-lab.its4u.eu
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-surprise
    weight: 100
  wildcardPolicy: None
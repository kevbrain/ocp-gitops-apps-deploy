kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: surprise-pipeline-claim
  namespace: surprise-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


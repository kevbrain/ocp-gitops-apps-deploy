apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/managed-by: EventListener
    app.kubernetes.io/part-of: Triggers
    eventlistener: matrix
  name: el-matrix
  namespace: {{ocp-namespace}}
spec:
  host: el-matrix-devops.apps.ocp-lab.its4u.eu
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-matrix
    weight: 100
  wildcardPolicy: None
kind: Service
apiVersion: v1
metadata:
  namespace: devops
  name: matrix-service
  labels:
    app: matrix
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: matrix
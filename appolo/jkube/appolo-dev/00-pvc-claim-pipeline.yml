kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: appolo-pipeline-claim
  namespace: appolo-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


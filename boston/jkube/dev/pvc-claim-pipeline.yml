kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: boston-pipeline-claim
  namespace: boston-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: chicago-pipeline-claim
  namespace: chicago-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


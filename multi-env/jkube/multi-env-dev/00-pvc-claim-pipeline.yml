kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: multi-env-pipeline-claim
  namespace: multi-env-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


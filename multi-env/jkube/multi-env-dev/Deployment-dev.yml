kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: multi-env-dev
  annotations:
    its4u.placeholders/auto: "true"
    reloader.stakater.com/auto: "true"
  name: multi-env
  labels:
    app: multi-env
spec:
  replicas: {{ocp.replicas}}
  selector:
    matchLabels:
      app: multi-env
  template:
    metadata:
      name: multi-env
      labels:
        app: multi-env
    spec:
      volumes:
        - name: emptydirlogs
          emptyDir: {}
      containers:
        - name: microservice-pod 
          resources:
            requests:
              memory: 250Mi
              cpu : 200m
            limits:
              memory: 750Mi
              cpu : 500m
          readinessProbe:
            httpGet:
              path: actuator/health
              port: 8080
              scheme: HTTP
          env:
            - name: ocp-cluster.registry
              valueFrom:
                configMapKeyRef:
                  name: multi-env-cm-0
                  key: ocp-cluster.registry
            - name: ocp.environment
              valueFrom:
                configMapKeyRef:
                  name: multi-env-cm-0
                  key: ocp.environment
            - name: ocp-namespace
              valueFrom:
                configMapKeyRef:
                  name: multi-env-cm-0
                  key: ocp-namespace
            - name: cluster-suffix
              valueFrom:
                configMapKeyRef:
                  name: multi-env-cm-0
                  key: cluster-suffix
            - name: ocp.replicas
              valueFrom:
                configMapKeyRef:
                  name: multi-env-cm-0
                  key: ocp.replicas
            - name: app-version
              valueFrom:
                configMapKeyRef:
                  name: multi-env-cm-0
                  key: app-version
            - name: actuator.password
              valueFrom:
                secretKeyRef:
                  name: multi-env-secret-0
                  key: actuator.password
          ports:
            - containerPort: 8080
              protocol: TCP
              name: http
          imagePullPolicy: Always
          volumeMounts:
            - name: emptydirlogs
              mountPath: /Logs
          image: >-
            {{ocp-cluster.registry}}/{{ocp-namespace}}/multi-env:{{app-version}}
      restartPolicy: Always
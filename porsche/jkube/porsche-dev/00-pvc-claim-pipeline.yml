kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: porsche-pipeline-claim
  namespace: porsche-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


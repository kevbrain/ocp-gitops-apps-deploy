kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: {{ocp-namespace}}
  annotations:
    its4u.placeholders/auto: "true"
    reloader.stakater.com/auto: "true"
  name: netscape
  labels:
    app: netscape
spec:
  replicas: {{ocp.replicas}}
  selector:
    matchLabels:
      app: netscape
  template:
    metadata:
      name: netscape
      labels:
        app: netscape
    spec:
      volumes:
        - name: emptydirlogs
          emptyDir: {}
      containers:
        - name: microservice-pod 
          resources:
            requests:
              memory: {{app-request.memory}}
              cpu : {{app-request.cpu}}
            limits:
              memory: {{app-limit.memory}}
              cpu : {{app-limit.cpu}}
          readinessProbe:
            httpGet:
              path: actuator/health
              port: {{app-actuator.port}}
              scheme: HTTP
          env:
            - name: ocp-cluster.registry
              valueFrom:
                configMapKeyRef:
                  name: netscape-cm-0
                  key: ocp-cluster.registry
            - name: ocp.environment
              valueFrom:
                configMapKeyRef:
                  name: netscape-cm-0
                  key: ocp.environment
            - name: ocp-namespace
              valueFrom:
                configMapKeyRef:
                  name: netscape-cm-0
                  key: ocp-namespace
            - name: cluster-suffix
              valueFrom:
                configMapKeyRef:
                  name: netscape-cm-0
                  key: cluster-suffix
            - name: ocp.replicas
              valueFrom:
                configMapKeyRef:
                  name: netscape-cm-0
                  key: ocp.replicas
            - name: app-version
              valueFrom:
                configMapKeyRef:
                  name: netscape-cm-0
                  key: app-version
            - name: actuator.password
              valueFrom:
                secretKeyRef:
                  name: netscape-secret-0
                  key: actuator.password
          ports:
            - containerPort: {{app-container.port}}
              protocol: TCP
              name: http
          imagePullPolicy: Always
          volumeMounts:
            - name: emptydirlogs
              mountPath: /Logs
          image: >-
            {{ocp-cluster.registry}}/{{ocp-namespace}}/netscape:{{app-version}}
      restartPolicy: Always
apiVersion: triggers.tekton.dev/v1alpha1
kind: Trigger
metadata:
  name: netscape-trigger
  namespace: devops
spec:
  serviceAccountName: pipeline
  bindings:
    - ref: netscape
  template:
    ref: netscape
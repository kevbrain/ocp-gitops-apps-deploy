apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/managed-by: EventListener
    app.kubernetes.io/part-of: Triggers
    eventlistener: netscape
  name: el-netscape
  namespace: devops
spec:
  host: el-netscape-devops.apps.ocp-lab.its4u.eu
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-netscape
    weight: 100
  wildcardPolicy: None
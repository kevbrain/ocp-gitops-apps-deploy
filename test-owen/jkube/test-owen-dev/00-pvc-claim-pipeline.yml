kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-owen-pipeline-claim
  namespace: test-owen-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: todelete-pipeline-claim
  namespace: todelete-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


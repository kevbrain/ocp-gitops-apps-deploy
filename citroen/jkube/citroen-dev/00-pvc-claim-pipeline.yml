kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: citroen-pipeline-claim
  namespace: citroen-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: importer-pipeline-claim
  namespace: importer-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


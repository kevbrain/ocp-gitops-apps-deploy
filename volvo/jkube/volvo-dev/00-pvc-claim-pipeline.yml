kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: volvo-pipeline-claim
  namespace: volvo-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


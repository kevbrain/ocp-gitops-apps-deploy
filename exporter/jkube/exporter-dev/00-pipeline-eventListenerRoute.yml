apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/managed-by: EventListener
    app.kubernetes.io/part-of: Triggers
    eventlistener: exporter
  name: el-exporter
  namespace: exporter-dev
spec:
  host: el-exporter-exporter-dev.apps.ocp-lab.its4u.eu
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-exporter
    weight: 100
  wildcardPolicy: None
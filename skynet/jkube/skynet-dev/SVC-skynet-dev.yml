kind: Service
apiVersion: v1
metadata:
  namespace: devops
  name: skynet-service
  labels:
    app: skynet
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: skynet
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/managed-by: EventListener
    app.kubernetes.io/part-of: Triggers
    eventlistener: skynet
  name: el-skynet
  namespace: {{ocp-namespace}}
spec:
  host: el-skynet-devops.apps.ocp-lab.its4u.eu
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-skynet
    weight: 100
  wildcardPolicy: None
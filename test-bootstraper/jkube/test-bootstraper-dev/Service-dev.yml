kind: Service
apiVersion: v1
metadata:
  namespace: test-bootstraper-dev
  name: test-bootstraper-service
  labels:
    app: test-bootstraper
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: test-bootstraper
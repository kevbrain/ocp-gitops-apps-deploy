kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: fortrash-pipeline-claim
  namespace: fortrash-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:


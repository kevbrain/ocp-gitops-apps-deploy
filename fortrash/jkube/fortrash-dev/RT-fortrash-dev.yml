kind: Route
apiVersion: route.openshift.io/v1
metadata:
  namespace:fortrash-dev
  name: fortrash-route
  labels:
    app: fortrash
spec:
  to:
    kind: Service
    name: fortrash-service
    weight: 256
  port:
    targetPort: 8080
  tls:
    termination: edge
  wildcardPolicy: None
---
